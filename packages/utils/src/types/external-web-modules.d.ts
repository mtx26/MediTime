// Legacy type declarations - web-specific imports have been replaced by
// configureApi() dependency injection pattern in apiConfig.ts.
// These declarations are kept empty in case any old type-checking references remain.
