// Web-only: cropImage and imageValidation use DOM APIs (HTMLImageElement, canvas, File)
// Import them directly from '@meditime/utils/src/files/cropImage' in web code if needed.
// They are excluded from the main barrel to avoid breaking React Native.
export {};
